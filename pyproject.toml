[project]
name = "data-preparation-pipeline-agent"
version = "2.0.0"
description = "OpenEnv data preparation pipeline agent"
requires-python = ">=3.10"

dependencies = [
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"